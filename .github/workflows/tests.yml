name: test

on: [push, pull_request]

jobs:
  project:
    name: test
    runs-on: ubuntu-20.04
    steps:
    - name: "run"
      run: echo ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
